<Window x:Class="TranTienDatWPF.Views.Customer.CustomerLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TranTienDatWPF.Views.Customer"
        mc:Ignorable="d"
        Title="Customer Login" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Số điện thoại" />
            <TextBox x:Name="txtPhone"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Mật khẩu" />
            <PasswordBox x:Name="txtPassword"/>
        </StackPanel>

        <Button Content="Đăng nhập" Grid.Row="2" Width="100" HorizontalAlignment="Center"
                Click="BtnLogin_Click"/>
    </Grid>
</Window>
