<Window x:Class="TranTienDatWPF.Views.Customer.EditCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TranTienDatWPF.Views.Customer"
        mc:Ignorable="d"
        Title="Edit Profile" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Tên công ty:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtCompanyName" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Tên người liên hệ:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtContactName" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Chức vụ:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtContactTitle" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Địa chỉ:" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAddress" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Lưu" Width="80" Height="30"  Margin="10,0" Click="BtnSave_Click"/>
            <Button Content="Hủy" Width="80" Height="30"  Margin="10,0" Click="BtnCancel_Click"/>
        </StackPanel>

    </Grid>
</Window>
