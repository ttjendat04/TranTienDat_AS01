<Window x:Class="WpfApp.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="Add Product" Height="300" Width="300"
         WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Product Name"/>
            <TextBox x:Name="txtProductName"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Unit Price"/>
            <TextBox x:Name="txtPrice"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Units In Stock"/>
            <TextBox x:Name="txtStock"/>
        </StackPanel>

        <Button Content="Save" Grid.Row="4" Width="80" Click="BtnSave_Click" HorizontalAlignment="Center" Margin="10"/>
  

    </Grid>
</Window>
